/* takes in a filepath and a pointer to int to store result, returns error code
  result is an avgerage length of subsequances where every element is divisable by 5
  error codes: descr
  0 - success
  1 - error opening a file
  2 - not enough data */
int Proccess(char* filepath, int*);
